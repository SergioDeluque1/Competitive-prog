//intNode.cpp
#include "intnode.h"
using namespace IntNode;

intNode(): element(0), next(nullptr){}
intNode (int value): element(value), next(nullptr){}

int intlist :: getsize(){}

void setElement (int value) {
  element = value};
void setNext (intNode* ref) {
  next = ref};
